# Cloudflare Pages Configuration
# This file is used for local development with Wrangler
# For production, ALL environment variables should be set in Cloudflare Dashboard

name = "bitcoin-investments"
compatibility_date = "2024-11-20"

# Pages configuration
pages_build_output_dir = "dist"

# NOTE: Do NOT define [vars] here for Pages projects!
# Environment variables should be set in:
# Cloudflare Dashboard > Workers & Pages > Your Project > Settings > Environment Variables
#
# For local development, use .dev.vars file instead
# See .dev.vars.example for the template
#
# Required environment variables (set in Dashboard):
# - VITE_SUPABASE_URL
# - VITE_SUPABASE_PUBLISHABLE_KEY
# - VITE_STRIPE_PUBLISHABLE_KEY
# - VITE_STRIPE_PRICE_MONTHLY
# - VITE_STRIPE_PRICE_ANNUAL
# - STRIPE_SECRET_KEY (secret)
# - STRIPE_WEBHOOK_SECRET (secret)
# - SUPABASE_SERVICE_ROLE_KEY (secret)
